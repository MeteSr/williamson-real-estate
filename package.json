{
  "name": "williamson-real-estate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build step required'",
    "start": "dfx start --background",
    "deploy:local": "dfx deploy",
    "deploy:testnet": "dfx deploy --network testnet",
    "deploy:mainnet": "dfx deploy --network ic",
    "stop": "dfx stop"
  }
}
